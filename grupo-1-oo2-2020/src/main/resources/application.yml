spring:
  datasource:
    url: jdbc:mysql://localhost:3306/grupo1bddoo22020?serverTimezone=UTC
    username: root
    password: 2122
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      dialect: org.hibernate.dialect.MySQL5Dialect
  thymeleaf:
    cache: false

